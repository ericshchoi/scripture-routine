# 청운교회 피택자 신약 필사 진도 체크 — V2

좋아요 확정 UI 반영 버전입니다.


아이폰/안드로이드 홈 화면에 설치할 수 있는 신약 필사 계획 앱입니다.

## 포함 기능
- 2026-08-10 마가복음 8장부터 2026-12-31 요한계시록 22장까지 계획
- 일요일 제외 주 6일
- 오늘 분량, 완료 체크, 진도율, 연속 필사일
- 알림 요일/시간 설정 UI
- Service Worker / PWA 설치
- Web Push 구독 + Node 서버 예제

## 주의
날짜별 분량은 현재 **절 수 균등 배분**입니다. 개역개정 본문 텍스트는 저작권 때문에 앱에 포함하지 않았습니다.
정확한 '글자 수 균등 배분'을 하려면 합법적으로 사용할 수 있는 절별 글자 수 메타데이터를 별도로 준비해야 합니다.

## 로컬 실행
```bash
npm install
npm start
```
브라우저에서 http://localhost:3000

## 실제 푸시 알림
Web Push는 HTTPS 배포가 필요합니다.
1. VAPID 키 생성: `npx web-push generate-vapid-keys`
2. 환경 변수 설정
   - VAPID_PUBLIC_KEY
   - VAPID_PRIVATE_KEY
   - VAPID_SUBJECT=mailto:본인이메일
3. HTTPS 지원 호스팅(Render/Railway/Fly.io 등)에 배포
4. 실제 서비스에서는 in-memory 구독 저장소를 Supabase/Postgres 등으로 교체

### iPhone
iOS 16.4+에서 홈 화면에 추가한 웹앱은 Web Push를 지원합니다. 앱 안의 '알림 설정 저장' 버튼처럼 사용자의 직접 동작에서 알림 권한을 요청해야 합니다.

## 다음 버전 권장
- 로그인/기기간 동기화
- Supabase 영구 저장
- 오늘 분량을 푸시 본문에 직접 표시
- 못 쓴 날의 분량을 절 단위로 자동 재배분
- 월간 캘린더
- 메모/묵상 기록


## 확정 UI 변경
- 청운교회 피택자 신약 필사 진도 체크 타이틀
- 진도율 기반 계단/십자가/기도하는 아이 위치 자동 이동
- 현재 완료 위치 입력 후 남은 일정 자동 재계산
- 오늘의 필사 카드
- 알림 설정 / 전체 계획 / 진도 현황 접이식 메뉴
- 모바일 하단 네비게이션


## V2.1 캐시/초기화 수정
- CSS/JS/scripture 데이터 파일에 버전 쿼리 적용
- 새 Service Worker 즉시 활성화(skipWaiting/clientsClaim)
- 예전 캐시 자동 삭제
- 모든 GET 요청을 network-first로 변경
- scripture 데이터 로드 실패 시 명확한 오류 화면 표시

## V2.2 확정 디자인 반영
- 사용자가 선택한 목업과 동일한 상단 정보 구조/정렬
- Noto Serif KR 기반 메인 타이틀
- 구름/햇살/계단/십자가 광원 재디자인
- SVG 기도하는 아이 캐릭터로 교체
- 아이 위치는 실제 신약 필사 진도율에 따라 동적으로 이동
- 기존 현재진도/자동재계산/오늘필사/알림/전체계획 기능 유지


## V2.3 말씀 수정
- 시편 119:105 표시 문구를 개역개정 문구로 수정: “주의 말씀은 내 발에 등이요 내 길에 빛이니이다”

## V2.4 확정 그래픽
- 승인한 계단/구름/기도하는 아이/십자가 광원 이미지를 실제 앱 hero 그래픽으로 반영
- 제목과 시편 119:105는 HTML 텍스트 유지
- 실제 진도율 숫자 배지는 그래픽 위에서 동적으로 이동
- 기존 진도 계산/오늘 필사/알림/전체 계획 기능 유지

## V2.5 실제 레이어 이동형
- 계단/구름/십자가: `journey-background.jpg` 고정 레이어
- 기도하는 아이: `praying-child.png` 투명 PNG 이동 레이어
- 현재 진도율: HTML 말풍선 이동 레이어
- 진도율 0~100%에 따라 아이와 진도 말풍선이 함께 계단을 따라 이동
- 0% / 100% 표시는 고정
- 모바일 제목은 `청운교회 피택자` 다음에 강제 줄바꿈
- 현재 진도 자동 재계산 및 기존 기능 유지

## V2.6 클린 레이어
- 고해상도 원본 시안에서 배경을 재구성해 `배경 레이어` 문구 등 불필요 요소 제거
- 아이를 머리~무릎/신발까지 포함한 고해상도 투명 PNG로 재추출
- 모바일 제목은 `청운교회 피택자` / `신약 필사 진도 체크` 두 줄 고정
- 아이와 진도 말풍선은 실제 진도율에 따라 계단을 따라 동적 이동

## V2.8 최종 클린
- 배경을 기존 완성 이미지에서 추출하지 않고 앱 전용 고해상도 그래픽으로 새로 제작
- 배경에는 제목/성구/아이/% 문구가 전혀 없음
- 계단은 벡터형 고해상도 렌더로 블러 제거
- 아이와 현재 퍼센트는 독립 레이어이며 실제 진도율에 따라 이동
- 모바일 제목은 청운교회 피택자 / 신약 필사 진도 체크 두 줄 고정

## V3.0 홈 화면 추가 안내
- 앱 하단에 '홈 화면에 앱 추가하기' 카드 추가
- 아이폰: Safari → 공유 → 홈 화면에 추가 안내
- 안드로이드: Chrome의 설치 프롬프트 지원 시 '지금 앱 설치하기' 버튼 제공
- 이미 홈 화면 앱으로 실행 중이면 설치 안내 자동 숨김

## V3.1
- Fold/일반 휴대폰/넓은 화면 hero 반응형 보정
- 권별 절 수 비례 treemap 진도 대시보드
- 각 권을 독립적으로 장/절까지 완료 입력 가능
- 완료 비율은 회색, 미완료는 초록색으로 표시
- 전체 진도율은 모든 권의 완료 절 수 합계로 계산, 계단 아이 위치에 반영
- 목표 완료일 사용자 지정 및 주 6일(일요일 제외) 계획 자동 재분배


## V3.3 반응형 아이 계단 위치
- 일반 스마트폰과 Z Fold 펼친 화면에 각각 다른 계단 이동 경로 적용
- 아이 레이어의 하단을 계단 좌표에 직접 앵커링하여 공중에 뜨거나 계단 아래로 빠지는 현상 수정
- 화면 회전/폴드 펼침·접힘 시 아이 위치 자동 재계산
- V3.2의 권별 진도, 목표일, 필사량 계산 기능은 그대로 유지

## V3.3.1 긴급 수정
- V3.3 app.js의 잘못 붙은 문자열로 인한 JavaScript 문법 오류 수정
- 권별 진도 블록, 목표일, 오늘의 필사 등 V3.2 기능 정상 복구
- 일반폰 / Z Fold 반응형 아이 계단 위치 조정은 유지
- 캐시 버전 갱신
